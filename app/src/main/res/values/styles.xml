<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppBaseTheme" />

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="colorControlNormal">@color/black</item>
        <item name="android:windowBackground">@color/bg_color</item>
    </style>

    <style name="RotateLoadingProgressStyle">
        <item name="android:indeterminateDrawable">@drawable/loading_dialog_rotate_loading</item>
        <item name="android:minWidth">45dp</item>
        <item name="android:maxWidth">45dp</item>
        <item name="android:minHeight">45dp</item>
        <item name="android:maxHeight">45dp</item>
    </style>

</resources>
